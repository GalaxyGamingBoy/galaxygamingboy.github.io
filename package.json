{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "genProd": "npx nuxi generate"
  },
  "devDependencies": {
    "nuxt": "^3.3.1",
    "sass": "^1.60.0",
    "sass-loader": "^13.2.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
